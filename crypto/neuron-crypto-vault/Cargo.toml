[package]
name = "neuron-crypto-vault"
version = "0.4.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Stub crypto provider for Vault Transit engine"
readme = "README.md"
categories = ["asynchronous", "cryptography"]
keywords = ["neuron", "ai", "agent", "vault", "crypto"]

[dependencies]
neuron-crypto = { path = "../neuron-crypto", version = "0.4.0" }
async-trait = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
