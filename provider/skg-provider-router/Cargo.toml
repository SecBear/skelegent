[package]
name = "skg-provider-router"
version = "0.1.0"
edition = "2021"
description = "Model routing provider for skelegent — selects provider per-request based on pluggable policy"

[dependencies]
skg-turn = { path = "../../turn/skg-turn" }
async-trait = "0.1"
thiserror = "2"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
layer0 = { path = "../../layer0" }
futures-util = "0.3"
